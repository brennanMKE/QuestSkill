---
description: Create, inspect, update, audit, or clear the current project's persistent Quest
---

Load the `quest` skill and handle this command using its `/quest` command rules.

The text supplied after `/quest` is:

```text
$ARGUMENTS
```

Treat an empty argument as a request for Quest help. Preserve the complete argument text when it is an objective; do not split or paraphrase it.
