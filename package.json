{
  "name": "opencode-quest",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && bash tests/plugin-runtime.test.sh && bash tests/install.test.sh",
    "check": "node --check quest/plugin/quest.js && node --test && bash tests/plugin-runtime.test.sh && bash tests/install.test.sh"
  }
}
