{
  "name": "opencode-quest",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test && bash tests/install.test.sh",
    "check": "node --check quest/plugin/quest.js && node --test && bash tests/install.test.sh"
  }
}
